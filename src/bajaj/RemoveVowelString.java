package bajaj;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Stream;

public class RemoveVowelString {
	public static void main(String[] args) {
		String str1="Hi am learning spring auth2";
		Stream<String> stream = Stream.of(str1);
		List<String> asList = Arrays.asList("a","e","i","o","u");
//		List<String> list = List.of(str1);
//		
//		 str1.chars().mapToObj(x->(char)x).map(x->{
//			).collect(Collectors.joining());
		
		
		
		
	}
}
